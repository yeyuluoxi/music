import Vue from 'vue'
import Vuex from "vuex"

Vue.use(Vuex)

const state = {
	username: "test",
};

const mutations = {
	
};

const actions = {
	
};

export default new Vuex.Store({
	state:state,
	mutations:mutations,
	actions:actions
})